'use strict';

/* Controllers */
angular.module('cms.admin_pages', []).
  controller('AdminPages', ['$scope', '$http', '$location', '$route', '$routeParams',
        function($scope, $http, $location, $route, $routeParams) {
        //$scope.users = [];
            console.log('loaded');
        }]);